Erick Branner
README.txt for assignment1-movies
12 October 2020

**Compilation**
COMPILE with `make`
Executable `movies` will be created
EXECUTE with `./movies some_file.csv`


**Other useful information**
There is at least one double free.
Execution works on OSU's os1 server,
but may not work on different machines

**Sources**
Used for reference on reading a string with spaces:
Also used for clearing the input buffer
S1: https://www.includehelp.com/c/c-program-to-read-string-with-spaces-using-scanf-function.aspx
Cited as `//S1` within code (prog.c)

**Documentation templates**

/**
 * Prgram Filename:
 * Author:
 * Date:
 * Description
 * Input:
 * Output:
 *
*/

/**
 * Function:
 * Description:
 * Parameters:
 * Pre-Conditions:
 * Post-Conditions:
 */
